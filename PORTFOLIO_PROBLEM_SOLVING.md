# MonsterHunterWorld 포트폴리오 - 클라이언트 시스템 개발 과정

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | MonsterHunterWorld 모작 프로젝트 |
| 장르 | 3D 멀티플레이 액션 RPG / 몬스터 헌터 스타일 모작 |
| 개발 인원 | 4인 팀 프로젝트 |
| 개발 기간 | 2025.03.14 ~ 2025.04.03 |
| 담당 역할 | 팀장, 아이템 데이터 구조, 인벤토리 UI, 사물함/장비 인벤토리, 퀵슬롯, 함정 아이템, Firebase 저장/로드 |
| 사용 기술 | Unity, C#, Photon PUN2, Firebase, GitHub |

몬스터 헌터 스타일의 아이템 관리 구조를 모작하면서, 아이템 데이터 로딩부터 인벤토리 UI, 장비 장착, 퀵슬롯, 함정 아이템 사용, 저장/로드까지 이어지는 클라이언트 시스템을 구현했습니다.

이 문서는 단순히 구현 기능을 나열하는 것이 아니라, 개발 과정에서 발생한 데이터 구조 문제와 예외 상황을 어떤 기준으로 해결했는지를 중심으로 정리했습니다.

![게임 대표 플레이 화면](https://github.com/user-attachments/assets/81ac5779-37da-4afa-b2ce-3fa385ff26c8)

---

## 2. 담당 범위와 초기 목표

### 담당 범위

- CSV 기반 아이템 데이터 로딩
- `ItemDataBase` 기반 아이템 검색 구조
- 일반 인벤토리 / 사물함 / 장비 인벤토리 / 장착 인벤토리 구조
- 인벤토리 슬롯 UI 갱신
- 아이템 획득, 이동, 교환 실패 복구
- 장비 장착 조건 판단
- 퀵슬롯 자동 세팅 및 소비 아이템 사용
- 함정 아이템 네트워크 생성 및 제거 처리
- Firebase 기반 인벤토리 저장/로드

### 초기 목표

아이템 정보를 코드에 직접 하드코딩하지 않고, 엑셀에서 관리하기 쉬운 CSV 형태로 분리하는 것이 목표였습니다.

CSV는 아이템을 사용할 때마다 계속 읽기 위한 파일이 아니라, 게임 시작 시 `ItemDataBase`를 만들기 위한 원본 데이터로 사용했습니다. 이후 실제 게임 플레이 중에는 CSV를 다시 읽지 않고, 미리 생성된 `ItemDataBase`에서 key를 기준으로 아이템 정보를 가져오는 구조를 목표로 했습니다.

또한 Firebase에는 아이템 이름, 설명, 능력치 같은 전체 정보를 저장하지 않고, 플레이어가 어떤 아이템을 몇 개 가지고 있는지만 알 수 있도록 아이템 ID와 수량 중심으로 저장해 데이터 전송량을 줄이고자 했습니다.

```text
CSV 원본 데이터
    ↓ 게임 시작 시 1회 로드
ItemDataBase 생성
    ↓ 런타임 조회
인벤토리 / 장비 / 퀵슬롯에서 key 기반 참조
    ↓ 저장 시
Firebase에는 ID와 수량 중심으로 저장
```

---

## 3. 개발 타임라인

| 시점 | 개발 흐름 |
|---|---|
| 03.19 | 인벤토리 UI 리소스 추가 및 배치 |
| 03.20 ~ 03.24 | 기본 인벤토리 제작, 아이템 획득/이동 구조 실험 |
| 03.26 | 사물함 인벤토리 추가, 인벤토리 간 이동 구조 확장 |
| 03.27 ~ 03.31 | 장비 인벤토리와 장비 장착 조건 처리 |
| 03.28 | 순차 탐색 대신 key 기반 `ItemDataBase` 접근으로 리팩토링 |
| 04.01 | Firebase 인벤토리 저장/로드 추가 |
| 04.02 | 퀵슬롯, 함정 네트워크 생성 처리 |
| 04.03 | 퀵슬롯 마지막 아이템 예외, 함정 부모 오브젝트 삭제, 로딩 방어 처리 |

처음부터 완성된 구조가 나온 것이 아니라, 기능을 붙이고 실행하면서 리스트 참조, 빈 슬롯 처리, 네트워크 오브젝트 삭제, 신규 유저 초기화 같은 문제를 확인하고 보완했습니다.

---

## 4. 핵심 구현 과정

## 4-1. CSV 기반 ItemDataBase 구성

### 초기 의도

아이템 번호, 이름, 타입, 장비 슬롯, 능력치, 가격, 이미지 번호 등을 코드에 직접 작성하면 아이템이 늘어날수록 수정 비용이 커집니다.

그래서 아이템 데이터를 엑셀에서 수정하기 쉬운 CSV 형태로 관리하고, 게임 시작 시 CSV를 한 번 읽어 `ItemDataBase`를 구성했습니다.

![CSV 아이템 데이터 표](https://github.com/user-attachments/assets/e82872cb-12bd-4634-8380-61eefda69052)

### 구현 방식

```text
게임 시작
    ↓
CSVItemLoader가 CSV 파일 로드
    ↓
아이템 타입에 따라 Weapon / Armor / Potion / Trap 객체 생성
    ↓
ItemDataBase에 key 기준으로 등록
    ↓
런타임에서는 CSV를 다시 읽지 않고 ItemDataBase에서 조회
```

### 얻은 이점

- 아이템 정보를 코드에 하드코딩하지 않아도 됨
- 엑셀 기반으로 아이템 데이터를 수정하기 쉬움
- 아이템 정보는 클라이언트에 미리 생성해두고, Firebase에는 ID와 수량만 저장 가능
- 런타임에서는 CSV 반복 읽기 없이 key 기반 조회 가능

### 남은 한계와 개선 방향

CSV는 사람이 수정하기 쉽지만, 아이템 수가 많아질수록 초기 로딩 시 읽어야 할 데이터가 늘어날 수 있습니다. 또한 엑셀 수정 중 특정 항목이 비거나 순서가 밀리면 파서가 잘못된 값을 읽을 수 있습니다.

이 프로젝트에서는 기능 구현과 발표 일정을 우선해 CSV 암호화나 빌드 전 변환까지는 적용하지 않았습니다. 이후 규모가 커진다면 CSV를 그대로 런타임에서 파싱하기보다, 빌드 전에 ScriptableObject, 바이너리 파일, Addressable Asset 같은 게임용 데이터로 변환하고, 실행 중에는 변환된 데이터를 읽는 방식으로 개선할 수 있습니다.

---

## 4-2. 인벤토리 슬롯과 EmptyItem 구조

### 초기 의도

처음에는 인벤토리에 실제 아이템만 넣어두고, UI 슬롯에 순서대로 보여주면 된다고 생각했습니다.

예를 들어 회복약 2개와 고기 1개가 있다면 리스트에는 실제 아이템 2종류만 들어가고, UI에서는 앞에서부터 채우면 된다고 본 것입니다.

### 실행 중 확인한 문제

하지만 인벤토리 UI는 항상 정해진 슬롯 수를 가지고 있습니다.

```text
화면 슬롯: 20칸
실제 아이템 리스트: 3개
```

이 상태에서 UI가 20칸을 갱신하려고 하면, 4번째 슬롯부터는 가져올 데이터가 없습니다. 또 아이템을 사용하거나 이동해서 수량이 0이 되었을 때, 단순히 리스트에서 제거하면 뒤의 아이템들이 앞으로 당겨져 UI 위치가 계속 바뀔 수 있습니다.

### 해결 방식

빈 슬롯도 하나의 데이터로 취급하기 위해 `EmptyItem`을 사용했습니다.

```text
[회복약] [고기] [EmptyItem] [EmptyItem] [EmptyItem] ...
```

이렇게 하면 UI 슬롯 수와 아이템 리스트 수를 맞출 수 있고, 빈칸도 명확한 상태로 관리할 수 있습니다.

아래 이미지는 플레이어 인벤토리와 사물함 인벤토리가 함께 열린 상태입니다. 같은 화면에서 아이템이 들어간 슬롯과 빈 슬롯을 확인할 수 있어, EmptyItem 기반 빈 슬롯 처리와 인벤토리 간 이동 구조를 설명하는 데 함께 사용합니다.

![인벤토리와 사물함 UI 기본 상태](https://github.com/user-attachments/assets/7dfbef0a-ffac-4b51-8ef7-cc42577b1585)

![인벤토리와 사물함 UI 아이템 포함 상태](https://github.com/user-attachments/assets/cc1c48c8-dd0e-4ddc-94d6-b1fbb2a2ea97)

### 얻은 이점

- UI 슬롯 수와 데이터 리스트 수를 맞출 수 있음
- 빈 슬롯을 `null`이 아닌 명확한 상태로 표현 가능
- 아이템 제거 후에도 슬롯 구조가 깨지지 않음
- 저장/로드 이후 UI 갱신 중 예외 가능성 감소

### EmptyItem 공유 방식에 대한 판단

`EmptyItem`은 모든 빈 슬롯이 같은 상태여도 되는 값이므로, 하나의 기준 객체를 여러 빈칸에 공유하는 방식은 현재 프로젝트에서는 합리적이었습니다.

다만 `EmptyItem`이 수정 가능한 객체로 남아 있으면, 실수로 값이 변경되었을 때 모든 빈 슬롯에 영향을 줄 수 있습니다. 이후 개선한다면 `EmptyItem`을 읽기 전용처럼 다루거나, 빈 슬롯 상태를 별도 슬롯 데이터로 분리할 수 있습니다.

---

## 4-3. ItemDataBase 원본과 인벤토리 아이템 분리

### 문제 상황

`ItemDataBase`에 등록된 아이템 객체를 인벤토리에 그대로 넣으면, 같은 원본 객체를 여러 곳에서 참조할 수 있습니다.

이 경우 한 슬롯의 수량을 변경했을 때 다른 슬롯이나 원본 데이터에도 영향을 줄 수 있습니다.

### 해결 방식

인벤토리에 아이템을 넣을 때 `ItemDataBase`의 원본을 직접 넣지 않고, `Clone()`으로 복사본을 생성해 넣었습니다.

```text
ItemDataBase 원본 아이템
    ↓ Clone()
인벤토리용 아이템 복사본
    ↓
count 같은 런타임 상태는 복사본에서 관리
```

### 얻은 이점

- 원본 아이템 데이터와 플레이 중 변경되는 수량 상태를 분리
- 한 슬롯의 count 변경이 다른 슬롯에 전파되는 문제 방지
- 무기, 방어구, 포션, 함정처럼 타입별 데이터가 다른 아이템도 복사 가능

### 남은 한계와 개선 방향

타입별로 `Clone()`을 직접 구현했기 때문에, 새 필드가 추가되면 Clone 로직에도 반영해야 합니다. 이후 개선한다면 아이템의 고정 정보와 런타임 상태 정보를 더 명확히 분리하여, 수량이나 장착 여부 같은 플레이 중 상태만 별도 객체에서 관리할 수 있습니다.

---

## 4-4. 인벤토리 확장 구조

### 확장 과정

처음에는 일반 인벤토리 중심으로 구현했지만, 이후 사물함, 장비 인벤토리, 장착 인벤토리가 추가되었습니다.

인벤토리 종류가 늘어나면서 슬롯 갱신, 빈 슬롯 처리, 정렬, 아이템 이동 같은 공통 기능이 반복될 수 있었습니다.

### 해결 방식

공통 처리는 `BaseInventory`에 두고, 각 인벤토리는 필요한 차이만 따로 처리하도록 구성했습니다.

```text
BaseInventory
- 슬롯 리스트 관리
- EmptyItem 처리
- UI 갱신 기본 흐름
- 정렬 / 압축 처리

하위 인벤토리
- 일반 인벤토리
- 사물함 인벤토리
- 장비 인벤토리
- 장착 인벤토리
```

### 얻은 이점

- 인벤토리 종류가 늘어도 공통 슬롯 처리 재사용 가능
- 빈 슬롯 처리 기준 통일
- UI별 차이는 하위 클래스에서 별도로 대응 가능

### 남은 한계와 개선 방향

프로젝트 기간이 짧았기 때문에 `BaseInventory`가 데이터와 UI 갱신을 함께 담당하는 구조로 구현했습니다.

이후 개선한다면 역할을 다음처럼 나눌 수 있습니다.

```text
데이터 관리: 아이템 리스트, 슬롯 수, 빈 슬롯 상태 관리
화면 표시: 슬롯 이미지, 수량 텍스트, 툴팁 표시
처리 로직: 아이템 획득, 이동, 정렬, 장착 처리
```

이렇게 분리하면 UI 디자인을 바꿔도 아이템 이동 규칙을 덜 건드릴 수 있고, 아이템 이동/정렬 같은 처리 로직을 다른 UI에서도 재사용하기 쉬워집니다.

---

## 4-5. 아이템 이동과 실패 복구

### 문제 상황

인벤토리에서 사물함으로 아이템을 옮길 때, 대상 인벤토리가 가득 차 있으면 이동이 실패할 수 있습니다.

이때 원본 인벤토리의 수량을 먼저 줄인 뒤 대상 인벤토리에 추가하는 구조라면, 추가 실패 시 아이템이 사라질 수 있습니다.

### 해결 방식

대상 인벤토리에 아이템 추가가 성공했는지 확인하고, 실패했다면 원본 아이템 수량을 복구하도록 처리했습니다.

```text
원본 인벤토리 수량 감소
    ↓
대상 인벤토리에 추가 시도
    ↓
성공: 이동 완료
실패: 원본 수량 복구
```

### 얻은 이점

- 인벤토리 간 이동 실패 시 아이템 유실 방지
- 대상 인벤토리가 가득 찬 상황에서도 데이터 안정성 확보
- 이동 성공/실패 흐름을 명확히 구분

### 이미지 사용

인벤토리와 사물함이 동시에 열린 이미지는 4-2 섹션의 기존 이미지를 사용합니다. 추가 촬영은 필요하지 않습니다.

### 개선 방향

이후 상점 구매/판매, 아이템 버리기, 장비 장착처럼 아이템 이동 규칙이 더 늘어난다면, 아이템 이동 처리를 별도 클래스로 분리해 여러 기능에서 같은 성공/실패 처리와 복구 로직을 재사용할 수 있습니다.

---

## 4-6. 장비 장착 조건 판단

### 구현 기준

장비 장착은 모작 대상 게임의 슬롯 구조에 맞춰 처리했습니다.

이 프로젝트에서는 장비 슬롯 종류가 명확하게 정해져 있었기 때문에, 슬롯을 무한히 확장하는 구조보다 잘못된 타입의 아이템이 잘못된 슬롯에 들어가지 않도록 조건을 명확히 하는 것이 더 중요했습니다.

### 처리 방식

- `Weapon`은 무기 슬롯에만 장착
- `Armor`는 자신의 `equipSlot`과 일치하는 방어구 슬롯에만 장착
- `EmptyItem`은 장비 해제와 빈 슬롯 표현을 위해 예외적으로 허용

```text
장비 아이템 선택
    ↓
Weapon / Armor 타입 확인
    ↓
장착 가능한 슬롯인지 검사
    ↓
가능하면 장착 인벤토리에 배치
    ↓
UI 갱신
```

### 얻은 이점

- 장비 아이템이 잘못된 슬롯에 들어가는 문제 방지
- 장비 해제 시에도 EmptyItem을 통해 빈 슬롯 상태 유지
- 모작 대상 게임의 고정된 장비 슬롯 구조에 맞게 구현 가능

### 이미지 사용

장비 장착 및 해제 화면은 이미 제공된 장비 슬롯 UI 이미지들을 사용합니다. 추가 촬영은 필요하지 않습니다.

---

## 4-7. 퀵슬롯 자동 세팅과 예외 처리

### 초기 의도

퀵슬롯에는 인벤토리의 모든 아이템을 넣는 것이 아니라, 플레이 중 바로 사용할 수 있는 포션과 함정류 아이템만 자동 등록하도록 구성했습니다.

![퀵슬롯 기본 상태](https://github.com/user-attachments/assets/d1fb0c40-3767-436f-a740-cdc2d13e7707)

### 문제 1: 순환 인덱스

퀵슬롯을 원형 리스트처럼 순환시키는 과정에서, 0번 아이템에서 이전 아이템으로 이동할 때 `currentIndex - 1`이 음수가 되는 문제가 있었습니다.

### 해결 1

아이템 개수를 더한 뒤 나머지 연산을 적용해 음수 인덱스가 나오지 않도록 처리했습니다.

```text
(currentIndex - 1 + items.Count) % items.Count
```

### 문제 2: 마지막 아이템 사용

퀵슬롯의 마지막 아이템을 사용하면 리스트가 비게 됩니다. 하지만 기존 로직은 여전히 현재 인덱스를 기준으로 아이템을 참조하려 했기 때문에, 아이템이 없는 상태에서 UI 갱신이나 아이템 접근이 발생할 수 있었습니다.

### 해결 2

- 아이템 수량이 0이 되면 리스트에서 제거
- 제거 후 남은 아이템이 없으면 `ClearUI()` 처리
- 아이템 리스트가 비어 있는 경우 `EmptyItem`을 기본값으로 사용
- 남은 아이템이 있다면 현재 인덱스를 유효 범위로 보정

### 얻은 이점

- 퀵슬롯 순환 처리 안정화
- 마지막 아이템 사용 후 예외 방지
- 퀵슬롯 UI와 실제 아이템 리스트 상태 동기화

---

## 4-8. 함정 아이템 네트워크 생성과 제거 처리

### 초기 구현

퀵슬롯에서 함정 아이템을 사용하면 함정 프리팹을 생성하도록 구현했습니다.

처음에는 로컬 오브젝트 생성으로 시작했지만, 멀티플레이 환경에서는 다른 플레이어도 같은 함정을 확인해야 했기 때문에 `PhotonNetwork.Instantiate`를 통해 네트워크 오브젝트로 생성하도록 수정했습니다.

### 실행 중 확인한 문제

처음에는 함정 설치 완료 처리, 몬스터 충돌 감지, 일정 시간 후 제거 처리를 `TrapCtrl` 하나에서 처리하려 했습니다.

하지만 함정 프리팹은 설치 중 모델과 설치 완료 모델이 나뉘어 있었고, 실제 충돌 판정은 자식 오브젝트 쪽에서 처리되는 구조였습니다.

이 상태에서 자식 오브젝트만 기준으로 삭제하면 부모 함정 오브젝트가 네트워크상에 남을 수 있었습니다.

### 해결 방식

역할을 두 스크립트로 분리했습니다.

| 스크립트 | 역할 |
|---|---|
| `TrapCtrl` | 설치 애니메이션 종료 시 완료 파츠 활성화 |
| `TrapCtrl2` | 자식 오브젝트에서 몬스터 충돌 감지 후 부모 `PhotonView`를 찾아 부모 함정 오브젝트 삭제 |

```text
함정 사용
    ↓
PhotonNetwork.Instantiate로 함정 생성
    ↓
TrapCtrl이 설치 애니메이션 종료 후 완료 파츠 활성화
    ↓
TrapCtrl2가 몬스터 충돌 감지
    ↓
부모 PhotonView 소유권 확인
    ↓
PhotonNetwork.Destroy로 부모 함정 오브젝트 삭제
```

### 얻은 이점

- 함정 오브젝트를 네트워크상에 생성 가능
- 설치 완료 처리와 충돌 후 삭제 처리를 분리
- 자식 충돌 판정 구조에서도 부모 함정 오브젝트가 남지 않도록 처리
- `PhotonView.IsMine` 기준으로 소유권이 있는 클라이언트만 삭제 수행

---

## 4-9. Firebase 저장/로드

### 구현 의도

Firebase는 아이템 전체 정보를 저장하는 용도가 아니라, 플레이어가 어떤 아이템을 몇 개 가지고 있는지 저장하는 용도로 사용했습니다.

아이템의 이름, 설명, 능력치, 이미지 번호 같은 고정 정보는 클라이언트의 `ItemDataBase`에 있고, Firebase에는 아이템 ID와 수량 중심으로 저장했습니다.

![Firebase 계정 데이터 화면](https://github.com/user-attachments/assets/0b3d54bd-6f1f-4e75-b00b-4a9c37f9a6e2)

![Firebase 인벤토리 데이터 화면](https://github.com/user-attachments/assets/fd91cb38-51aa-4da0-8d87-ba09813d3241)

### 저장/로드 흐름

```text
인벤토리 리스트
    ↓
ID / count 중심의 문자열 데이터로 변환
    ↓
Firebase 저장
    ↓
로그인 시 Firebase에서 로드
    ↓
ID를 기준으로 ItemDataBase에서 아이템 정보 복원
    ↓
인벤토리 리스트와 UI 갱신
```

### 개발 중 확인한 문제

기존 테스트 계정에는 이미 저장된 인벤토리 데이터가 있었기 때문에, `ParseCSVToItemList()`를 통해 리스트가 정상적으로 복원되었습니다.

하지만 Firebase 프로젝트를 교체하거나 신규 유저로 로그인하는 상황에서는 저장된 인벤토리 데이터가 없었습니다. 이 경우 파싱 로직이 실행되지 않아 인벤토리 리스트가 슬롯 수만큼 초기화되지 않았고, 이후 빈 슬롯 보정이나 UI 갱신 과정에서 `NullReferenceException`이 발생할 수 있었습니다.

### 정리한 기준

- 저장 데이터가 있으면 기존 데이터를 복원
- 저장 데이터가 없으면 기본 인벤토리 슬롯을 생성
- UI 갱신 전에 리스트 크기와 `EmptyItem` 상태를 먼저 보장
- 신규 유저 초기화와 기존 유저 데이터 복원을 분리해서 처리

### 클라이언트 관점에서 얻은 점

Firebase 자체보다 중요한 것은, 외부 저장 데이터가 항상 존재한다고 가정하면 안 된다는 점이었습니다. 저장 데이터 유무에 따라 클라이언트 초기화 흐름을 나누고, UI 갱신 전에 사용할 데이터 상태를 먼저 보장해야 한다는 기준을 정리했습니다.

---

## 4-10. UI Stack과 버튼 등록

### 구현 의도

몬스터 헌터 스타일의 UI는 인벤토리, 아이템 관리, 교환, 확인 창처럼 단계적으로 열리는 구조입니다.

단순히 `SetActive`만 사용하면 ESC 입력 시 어떤 UI를 먼저 닫아야 하는지 추적하기 어렵기 때문에, 열린 순서를 관리할 필요가 있었습니다.

### 해결 방식

`UIManager`에서 Stack 구조를 사용해 열린 UI 순서를 저장하고, ESC 입력 시 마지막에 열린 UI부터 닫히도록 구성했습니다.

```text
UI A 열기 → Stack Push
UI B 열기 → Stack Push
ESC 입력 → UI B 닫기
ESC 입력 → UI A 닫기
```

또한 버튼마다 직접 UnityEvent를 연결하는 반복 작업을 줄이기 위해, `UIButtonManager`에서 버튼과 다음 UI를 Inspector에서 연결할 수 있도록 구성했습니다.

### 얻은 이점

- 중첩 UI에서도 뒤로가기 흐름을 명확히 제어
- ESC 입력 시 마지막에 열린 UI부터 닫힘
- 버튼 연결 반복 작업 감소
- UI 흐름 관리 편의성 향상

---

## 5. 남은 한계와 개선 방향

| 항목 | 현재 방식 | 개선 방향 |
|---|---|---|
| CSV 원본 데이터 | 엑셀 기반 CSV를 게임 시작 시 파싱 | 빌드 전 ScriptableObject / 바이너리 / Addressable Asset으로 변환, 암호화 또는 변조 방지 처리 |
| EmptyItem | 하나의 기준 객체를 빈 슬롯에 공유 | 읽기 전용처럼 다루거나 SlotData로 분리 |
| Clone 수동 구현 | 타입별 `Clone()` 직접 구현 | 아이템 고정 정보와 런타임 상태를 분리해 필드 누락 가능성 감소 |
| Firebase 로드 타이밍 | 비어 있으면 return하는 방어 처리 일부 사용 | 로드 완료 이벤트 이후 UI/퀵슬롯/장비 갱신이 실행되도록 순서 제어 |
| 인벤토리 책임 분리 | `BaseInventory`가 데이터와 UI 갱신을 함께 담당 | 데이터 관리, 화면 표시, 아이템 처리 로직 분리 |

---

## 6. 클라이언트 프로그래머 관점에서 어필할 점

### 데이터 기반 클라이언트 구조

CSV로 아이템 데이터를 외부화하고, 게임 시작 시 `ItemDataBase`를 구성한 뒤 런타임에서는 key 기반으로 조회했습니다. 이를 통해 하드코딩을 줄이고, 인벤토리/장비/퀵슬롯이 같은 아이템 DB를 참조하도록 만들었습니다.

### UI와 데이터 동기화

인벤토리 UI 슬롯 수와 아이템 리스트 크기가 어긋나지 않도록 `EmptyItem`을 기준으로 빈 슬롯을 관리했습니다. 이를 통해 저장/로드, 아이템 제거, 정렬 이후에도 UI 갱신이 안정적으로 동작하도록 구성했습니다.

### 원본 데이터와 런타임 상태 분리

`ItemDataBase`의 원본 아이템을 그대로 인벤토리에 넣지 않고, `Clone()`을 통해 인벤토리용 복사본을 생성했습니다. 이를 통해 원본 아이템 정보와 플레이 중 변하는 수량 상태를 분리했습니다.

### 예외 상황 대응

마지막 아이템 사용, 인벤토리 이동 실패, 신규 유저 저장 데이터 부재, 부모-자식 구조의 함정 오브젝트 삭제처럼 실제 플레이 흐름에서 발생할 수 있는 예외 상황을 확인하고 보완했습니다.

### 네트워크 오브젝트 처리 경험

함정 아이템을 `PhotonNetwork.Instantiate`로 생성하고, 충돌 이후에는 부모 `PhotonView` 소유권을 확인한 뒤 `PhotonNetwork.Destroy`로 제거했습니다. 단순 UI 아이템 사용을 네트워크 오브젝트 생성/삭제 흐름까지 연결한 경험을 얻었습니다.

---

## 7. 이미지 삽입 목록

### 현재 문서에 사용 중인 이미지

- 게임 대표 플레이 화면
- CSV 아이템 데이터 표
- 인벤토리와 사물함 UI 기본 상태
- 인벤토리와 사물함 UI 아이템 포함 상태
- 퀵슬롯 기본 상태
- Firebase 계정 데이터 화면
- Firebase 인벤토리 데이터 화면

### 기존 제공 이미지로 사용할 항목

- 장비 장착 및 해제 화면
- 장비 슬롯 UI 화면

### 추가 이미지 요청

현재 기준으로 추가 촬영이 필요한 이미지는 없습니다.
