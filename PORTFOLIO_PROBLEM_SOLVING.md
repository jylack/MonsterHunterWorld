# MonsterHunterWorld 포트폴리오 정리

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | MonsterHunterWorld 모작 프로젝트 |
| 장르 | 3D 멀티플레이 액션 RPG / 몬스터 헌터 스타일 모작 |
| 개발 기간 | 2025년 3월 ~ 2025년 7월 |
| 개발 인원 | 4인 팀 프로젝트 |
| 담당 역할 | 팀장, 아이템 시스템, 인벤토리 UI, CSV 데이터 연동, Firebase 기반 인벤토리 저장/로드 |
| 사용 기술 | Unity 2022.3.21f, C#, Photon PUN2, Firebase, GitHub |

Photon PUN2와 Firebase를 활용해 멀티플레이 환경에서 동작하는 몬스터 헌터 스타일의 팀 프로젝트를 제작했습니다.  
저는 팀장으로서 전체 기능 분담을 관리하면서, 아이템 데이터 구조, 인벤토리 UI, 장비 인벤토리, 퀵슬롯, Firebase 기반 인벤토리 저장/로드 기능을 담당했습니다.

이 프로젝트에서 중점적으로 보여주고자 하는 역량은 단순히 아이템 UI를 구현한 것이 아니라, **CSV 데이터 → 런타임 아이템 객체 → 인벤토리 리스트 → UI 슬롯 → Firebase 저장/로드**로 이어지는 데이터 흐름을 직접 설계하고, 개발 중 발생한 예외 상황을 수정하며 구조를 안정화한 경험입니다.

[이미지요청: 게임 플레이 화면 또는 프로젝트 대표 스크린샷]

---

## 2. 담당 기능 요약

| 담당 기능 | 구현 내용 | 보여줄 수 있는 역량 |
|---|---|---|
| CSV 기반 아이템 데이터 로딩 | 무기, 방어구, 소비 아이템, 함정 아이템 데이터를 CSV로 관리하고 런타임에 파싱 | 데이터 기반 구조 설계, 하드코딩 최소화 |
| 인벤토리 / 사물함 / 장비 인벤토리 | 여러 인벤토리 타입을 슬롯 UI와 연결하고, EmptyItem 기준으로 빈 슬롯 관리 | UI와 데이터 리스트 동기화, 공통 구조 설계 |
| Firebase 저장/로드 | 인벤토리 데이터를 문자열 형태로 저장하고, 로드 시 다시 아이템 리스트로 복원 | 외부 DB 연동, 저장 데이터 복원 흐름 설계 |
| 신규 유저 초기화 문제 점검 | 저장 데이터가 없는 신규 유저에서 인벤토리 리스트가 초기화되지 않는 문제를 분석 | 예외 상황 분석, 초기화 책임 분리 |
| 퀵슬롯 아이템 사용 | 소비 아이템/함정 아이템을 퀵슬롯에서 사용하고 UI에 반영 | 런타임 상태 변화 처리, 예외 처리 |
| 장비 장착 / 해제 | 장비 슬롯에 따라 아이템을 장착하고 캐릭터 상태와 외형에 반영 | 데이터와 캐릭터 표현 연결, 상태 관리 |

---

## 3. 핵심 기능 상세

### 3-1. CSV 기반 아이템 데이터 로딩 구조

#### 목표

아이템 정보를 코드에 직접 작성하지 않고 CSV 파일로 분리하여, 아이템 추가와 밸런스 수정이 코드 수정 없이 가능하도록 구성하는 것이 목표였습니다.

#### 초기 상황

몬스터 헌터 스타일의 게임에서는 무기, 방어구, 소비 아이템, 함정 아이템처럼 서로 다른 속성을 가진 아이템이 필요했습니다.  
이를 모두 코드에 직접 작성하면 아이템 수가 늘어날수록 수정 비용이 커지고, 팀원이 데이터를 확인하거나 수정하기 어려워질 수 있었습니다.

#### 해결 방식

아이템 공통 정보와 타입별 정보를 CSV 컬럼으로 정의하고, 런타임에 CSV를 읽어 아이템 객체로 변환하는 방식을 사용했습니다.

예를 들어 CSV에는 다음과 같은 정보가 포함됩니다.

- 아이템 ID
- 이름
- 타입
- 장비 슬롯
- 희귀도
- 공격력 / 방어력
- 속성 방어 수치
- 최대 보유 수량
- 설명
- 가격
- 회복량 / 스태미나 회복량
- 이미지 인덱스

개발 중 소비 아이템의 효과를 확장하면서 `heal`, `maxHeal`, `stamina`, `maxStamina` 같은 컬럼을 추가했고, CSV 파서도 해당 컬럼을 읽어들이도록 수정했습니다.

#### 결과

아이템 데이터가 코드와 분리되면서 아이템 추가 및 수정이 쉬워졌고, 무기/방어구/소비 아이템/함정 아이템을 하나의 데이터 흐름에서 관리할 수 있었습니다.

#### 보여줄 수 있는 역량

- 데이터 중심 설계
- CSV 파싱 구조 구현
- 아이템 타입별 데이터 분기 처리
- 기능 확장에 따른 데이터 구조 수정 경험

[이미지요청: item_data.CSV 일부 화면]

---

### 3-2. 인벤토리 슬롯과 EmptyItem 기반 빈 슬롯 처리

#### 목표

인벤토리 UI는 정해진 슬롯 수를 가지고 있기 때문에, 실제 아이템이 없는 칸도 빈 슬롯으로 안정적으로 표시되어야 했습니다.  
따라서 데이터 리스트와 UI 슬롯 수가 어긋나지 않도록 관리하는 것이 핵심이었습니다.

#### 문제 상황

초기에는 인벤토리 리스트에 실제 아이템만 들어가도 UI를 표시할 수 있을 것으로 생각했습니다.  
하지만 UI 슬롯은 고정된 개수를 가지고 있고, 리스트는 아이템 개수에 따라 길이가 달라질 수 있었습니다.

이 상태에서는 다음 문제가 발생할 수 있었습니다.

- UI 슬롯 인덱스와 아이템 리스트 인덱스가 맞지 않음
- 빈 슬롯을 표시해야 하는데 리스트에 해당 인덱스가 없음
- 아이템 제거 후 빈 공간이 제대로 보정되지 않음
- 저장/로드 이후 슬롯 수가 보장되지 않으면 UI 갱신 중 예외 발생 가능

#### 해결 방식

빈 슬롯을 단순 `null`로 두지 않고, `EmptyItem`이라는 기준 아이템을 사용했습니다.  
이를 통해 실제 아이템이 없는 슬롯도 하나의 명확한 상태로 표현할 수 있게 했습니다.

구조적으로는 다음 흐름을 기준으로 정리했습니다.

1. 인벤토리 슬롯 수를 기준으로 리스트 크기를 보장한다.
2. 실제 아이템이 없는 칸은 `EmptyItem`으로 채운다.
3. UI 갱신 시 리스트 값이 없거나 null이면 `EmptyItem`으로 대체한다.
4. 아이템 수량이 0이 된 경우에도 빈 슬롯 상태로 정리한다.

#### 결과

인벤토리 UI와 데이터 리스트가 같은 기준으로 동작하게 되었고, 아이템 제거/정렬/저장/로드 후에도 슬롯 구조가 깨지지 않도록 관리할 수 있었습니다.

#### 보여줄 수 있는 역량

- UI 슬롯과 데이터 리스트 동기화 문제 해결
- null 대신 명시적 빈 객체를 사용하는 상태 관리
- 인벤토리 구조의 안정성 확보
- 예외 상황을 줄이기 위한 방어적 설계

[이미지요청: 인벤토리 빈 슬롯과 아이템 슬롯이 함께 보이는 화면]

---

### 3-3. Firebase 기반 인벤토리 저장/로드 구조

#### 목표

플레이어가 획득한 아이템과 장비 상태를 Firebase에 저장하고, 다시 접속했을 때 이전 인벤토리 상태를 복원하는 것이 목표였습니다.

#### 구현 흐름

인벤토리 데이터는 Firebase에 저장 가능한 형태로 변환한 뒤 저장하고, 로드 시 다시 아이템 리스트로 복원하는 방식으로 구성했습니다.

기본 흐름은 다음과 같습니다.

```text
인벤토리 리스트
    ↓
CSV 형태 문자열로 변환
    ↓
Firebase 저장
    ↓
Firebase에서 문자열 로드
    ↓
ParseCSVToItemList()로 아이템 리스트 복원
    ↓
인벤토리 UI 갱신
```

#### 개발 중 확인한 문제

Firebase 기반 인벤토리 저장/로드 구조를 점검하던 중, 기존 로직이 **저장된 CSV 인벤토리 데이터가 존재한다는 전제**에 의존하고 있음을 확인했습니다.

기존 테스트 계정에서는 이미 저장된 인벤토리 데이터가 있었기 때문에 `ParseCSVToItemList()`를 통해 리스트가 정상적으로 복원되었습니다.  
그러나 Firebase 프로젝트를 교체하거나 신규 유저로 로그인하는 상황에서는 저장된 인벤토리 데이터가 없어 파싱 로직이 실행되지 않았고, 그 결과 인벤토리 리스트가 슬롯 수만큼 초기화되지 않은 상태로 남을 수 있었습니다.

이 상태에서 빈 슬롯 보정 로직이 실행되면 리스트 내부 요소 접근 과정에서 `NullReferenceException`이 발생할 수 있었습니다.

#### 정리한 설계 기준

이 문제를 통해 Firebase 로드 로직은 저장 데이터의 존재 여부와 관계없이 다음 조건을 보장해야 한다는 기준을 정리했습니다.

- 신규 유저도 기본 인벤토리 슬롯 수만큼 리스트가 생성되어야 한다.
- 저장 데이터가 없더라도 빈 슬롯은 `EmptyItem` 기준으로 채워져야 한다.
- Firebase 로드 이후 UI 갱신 전에 리스트 크기와 빈 슬롯 상태가 먼저 보장되어야 한다.
- 데이터 복원 로직과 신규 유저 초기화 로직을 분리해서 생각해야 한다.

#### 결과

Firebase 저장/로드 기능을 단순히 기존 데이터를 복원하는 기능이 아니라, **기존 유저 데이터 복원과 신규 유저 기본 인벤토리 생성을 모두 포함하는 초기화 흐름**으로 바라보게 되었습니다.

#### 보여줄 수 있는 역량

- 외부 DB 저장/로드 흐름 설계
- 신규 유저와 기존 유저의 데이터 상태 차이 분석
- 저장 데이터 부재 상황에 대한 예외 처리 기준 정리
- 런타임 데이터 초기화 책임 분리 경험

[이미지요청: Firebase Realtime Database 또는 저장된 인벤토리 데이터 화면]

---

### 3-4. Firebase 연결 최소화와 저장 책임 분리

#### 목표

UI 조작과 Firebase 저장 호출이 불필요하게 결합되지 않도록 정리하는 것이 목표였습니다.

#### 문제 상황

개발 중 ESC 키로 UI를 닫는 동작에서 Firebase 저장까지 함께 호출되는 구조가 있었습니다.  
이 방식은 단순히 UI를 닫는 입력에서도 DB 저장이 실행될 수 있어, UI 제어와 데이터 저장 책임이 섞이는 문제가 있었습니다.

#### 해결 방향

ESC 키 입력은 UI 닫기만 담당하도록 하고, Firebase 저장은 별도의 저장 흐름에서 처리하도록 분리했습니다.

```text
기존 흐름
ESC 입력 → UI 닫기 → Firebase 저장

개선 방향
ESC 입력 → UI 닫기
저장 필요 시점 → Firebase 저장
```

#### 결과

입력 처리와 데이터 저장 책임이 분리되면서 불필요한 Firebase 접근을 줄이고, UI 조작이 저장 로직에 영향을 주지 않도록 정리할 수 있었습니다.

#### 보여줄 수 있는 역량

- 기능 책임 분리
- 외부 서비스 호출 최소화 관점
- UI 입력 처리와 데이터 저장 흐름 분리
- 유지보수 가능한 구조로 개선한 경험

---

### 3-5. 퀵슬롯 아이템 사용 후 예외 처리

#### 목표

퀵슬롯에 등록된 소비 아이템이나 함정 아이템을 전투 중 사용할 수 있도록 하고, 사용 후 수량과 UI가 정상적으로 갱신되도록 만드는 것이 목표였습니다.

#### 문제 상황

퀵슬롯 아이템을 모두 사용했을 때 리스트에서 아이템이 제거되면서 다음 문제가 발생했습니다.

- 현재 인덱스가 비어 있는 리스트를 참조할 수 있음
- 아이템이 없는 상태에서 UI 표시 함수가 실행될 수 있음
- 마지막 아이템 사용 후 UI가 초기화되지 않음
- 아이템 리스트가 비었는데도 기존 인덱스를 기준으로 접근할 수 있음

#### 해결 방식

아이템 리스트가 비어 있는 경우 `EmptyItem`을 기본값으로 사용하고, 아이템 수량이 0이 되면 리스트에서 제거한 뒤 남은 아이템 수를 다시 확인했습니다.  
남은 아이템이 없으면 퀵슬롯 UI를 초기화하고, 아이템이 남아 있으면 현재 인덱스를 유효 범위로 보정했습니다.

#### 결과

퀵슬롯 아이템을 모두 사용한 경우에도 예외가 발생하지 않고, UI가 빈 상태로 안정적으로 전환되도록 수정했습니다.

#### 보여줄 수 있는 역량

- 런타임 리스트 변경에 따른 인덱스 예외 처리
- 마지막 아이템 사용 상황까지 고려한 UI 상태 관리
- 소비 아이템 수량과 UI 표시 동기화
- 게임 플레이 중 발생하는 상태 변화 디버깅 경험

[이미지요청: 퀵슬롯에 아이템이 있는 상태 / 모두 사용 후 빈 상태 비교]

---

### 3-6. 장비 장착 및 해제 구조

#### 목표

무기와 방어구를 장비 슬롯에 장착하고, 장착 상태를 캐릭터 스탯 및 외형에 반영하는 것이 목표였습니다.

#### 개발 중 확인한 문제

장비 장착 기능을 구현하는 과정에서 장비 파츠의 적용 순서와 머티리얼 세팅이 예상과 다르게 동작하는 문제가 있었습니다.  
장비 파츠가 섞이거나 머티리얼 설정이 깨지는 문제가 있었기 때문에, 장비 장착 흐름에서 어떤 파츠가 어떤 슬롯에 대응되는지 정리할 필요가 있었습니다.

#### 해결 방향

장비 아이템을 장비 슬롯 기준으로 분류하고, 장착/해제 시 인벤토리 상태와 장비 상태가 서로 어긋나지 않도록 정리했습니다.  
또한 장비 머티리얼과 외형 적용 문제를 수정하면서 장비 데이터와 캐릭터 표현이 연결되는 흐름을 점검했습니다.

#### 결과

장비 장착/해제 기능을 구현하면서 단순 UI 변경이 아니라, 아이템 데이터, 장비 슬롯, 캐릭터 상태, 외형 표현이 함께 맞물리는 구조를 경험했습니다.

#### 보여줄 수 있는 역량

- 장비 슬롯 기반 상태 관리
- 아이템 데이터와 캐릭터 표현 연결
- 장착/해제 시 데이터 불일치 방지
- 캐릭터 외형 적용 문제 디버깅 경험

[이미지요청: 장비 장착 전후 캐릭터 외형 또는 장비 UI 비교]

---

## 4. 기술적 판단 정리

### 4-1. CSV를 사용한 이유

아이템 데이터는 코드보다 표 형태로 관리하는 것이 수정과 확장에 유리하다고 판단했습니다.  
특히 무기, 방어구, 소비 아이템처럼 종류가 많은 데이터는 CSV로 분리하면 코드 수정 없이 값 조정이 가능하고, 팀원도 데이터를 확인하기 쉬웠습니다.

다만 CSV 컬럼이 늘어날수록 파서 코드도 함께 수정해야 하므로, 컬럼 순서와 누락값 처리에 주의해야 한다는 한계도 있었습니다.

### 4-2. EmptyItem을 사용한 이유

빈 슬롯을 `null`로 처리하면 UI 갱신, 정렬, 저장/로드 과정에서 매번 null 검사가 필요합니다.  
반면 `EmptyItem`을 기준값으로 사용하면 빈 슬롯도 하나의 아이템 상태로 다룰 수 있어 인벤토리 리스트와 UI 슬롯을 같은 구조로 관리하기 쉬웠습니다.

이 방식의 장점은 빈 슬롯 상태가 명확해진다는 점이고, 한계는 EmptyItem이 실제 아이템과 섞이지 않도록 타입과 처리 조건을 명확히 구분해야 한다는 점입니다.

### 4-3. Firebase 저장/로드에서 초기화 흐름이 필요한 이유

Firebase에 저장 데이터가 있는 기존 유저와 저장 데이터가 없는 신규 유저는 로드 결과가 다릅니다.  
따라서 로드 로직은 단순히 데이터를 가져오는 것에서 끝나면 안 되고, 데이터가 없을 때 기본 인벤토리를 생성하는 초기화 흐름까지 포함해야 합니다.

이를 통해 저장 데이터 유무와 관계없이 UI 갱신 전에 항상 동일한 리스트 구조를 보장할 수 있습니다.

### 4-4. UI 조작과 저장 호출을 분리한 이유

UI 닫기와 Firebase 저장이 함께 묶이면, 단순 UI 조작에서도 외부 DB 접근이 발생할 수 있습니다.  
따라서 UI 입력은 화면 상태 변경만 담당하고, 저장은 명확한 저장 시점에서만 호출하는 구조가 더 안정적이라고 판단했습니다.

---

## 5. 개발 중 겪은 시행착오와 개선 경험

### 5-1. 저장 데이터가 있는 계정만 기준으로 테스트한 문제

처음에는 기존 테스트 계정에 이미 인벤토리 데이터가 저장되어 있었기 때문에 로드 구조가 정상 동작하는 것처럼 보였습니다.  
하지만 Firebase 프로젝트를 교체하고 신규 유저로 로그인하자 저장 데이터가 없었고, 이때 인벤토리 리스트가 생성되지 않는 문제가 드러났습니다.

이 경험을 통해 기능 테스트에서는 정상 케이스뿐 아니라, 신규 유저 / 저장 데이터 없음 / 리스트 비어 있음 같은 초기 상태도 함께 확인해야 한다는 점을 배웠습니다.

### 5-2. 퀵슬롯 마지막 아이템 사용 문제

퀵슬롯에서 마지막 아이템을 사용하면 리스트가 비게 되는데, 기존 로직은 여전히 현재 인덱스를 기준으로 아이템을 참조하려 했습니다.  
이 문제를 수정하면서 리스트에서 요소를 제거한 뒤에는 반드시 남은 개수와 현재 인덱스를 다시 보정해야 한다는 점을 정리했습니다.

### 5-3. Firebase 연결 호출 위치 문제

UI를 닫는 동작에서 Firebase 저장까지 함께 처리하면 구조가 단순해 보이지만, 실제로는 입력 처리와 저장 책임이 섞입니다.  
이를 분리하면서 UI 상태 변경과 데이터 저장은 서로 다른 책임으로 관리해야 한다는 점을 확인했습니다.

### 5-4. 장비 파츠와 머티리얼 적용 문제

장비 장착 기능은 단순히 아이템을 슬롯에 넣는 것보다 복잡했습니다.  
장비 파츠의 순서, 머티리얼 세팅, 캐릭터 외형 반영이 함께 맞물렸고, 이 과정에서 데이터와 실제 오브젝트 적용 결과가 다를 수 있음을 경험했습니다.

---

## 6. 포트폴리오에서 강조할 역량

이 프로젝트를 통해 강조할 수 있는 역량은 다음과 같습니다.

1. **데이터 기반 아이템 시스템 설계**  
   CSV를 활용해 아이템 데이터를 코드와 분리하고, 타입별 아이템을 런타임 객체로 변환했습니다.

2. **UI와 데이터 동기화 경험**  
   인벤토리 슬롯, 아이템 리스트, EmptyItem 상태를 맞추며 UI와 데이터 구조가 어긋나지 않도록 관리했습니다.

3. **Firebase 저장/로드 구조 구현 경험**  
   인벤토리 데이터를 외부 DB에 저장하고 다시 복원하는 흐름을 구현했습니다.

4. **예외 상황 분석과 보완 경험**  
   신규 유저 저장 데이터 부재, 퀵슬롯 마지막 아이템 사용, count 0 아이템 처리 등 실제 플레이 흐름에서 발생할 수 있는 문제를 분석했습니다.

5. **기능 책임 분리 관점**  
   UI 입력과 Firebase 저장 호출을 분리하면서 기능별 책임을 명확히 나누는 방향으로 구조를 개선했습니다.

6. **팀 프로젝트 협업 경험**  
   팀장으로서 기능 분담과 개발 흐름을 관리하면서, 아이템 시스템이 UI, 플레이어 상태, Firebase 저장 구조와 연결되도록 작업했습니다.

---

## 7. 면접 설명용 요약

이 프로젝트에서 저는 팀장으로서 아이템 시스템과 인벤토리 UI, Firebase 저장/로드 기능을 담당했습니다.  
단순히 아이템을 표시하는 UI를 만든 것이 아니라, CSV로 관리되는 아이템 데이터를 런타임에 파싱하고, 이를 인벤토리 리스트와 UI 슬롯에 연결한 뒤 Firebase에 저장하고 다시 복원하는 흐름을 구현했습니다.

개발 과정에서 가장 크게 배운 부분은 저장 데이터가 항상 존재한다고 가정하면 안 된다는 점이었습니다.  
기존 테스트 계정에서는 저장된 CSV 데이터가 있어 정상적으로 보였지만, Firebase 프로젝트를 교체하거나 신규 유저로 로그인하면 저장 데이터가 없어서 인벤토리 리스트가 슬롯 수만큼 초기화되지 않는 문제가 발생할 수 있었습니다.  
이 문제를 통해 신규 유저와 기존 유저 모두에서 리스트 크기와 EmptyItem 기반 빈 슬롯 상태를 보장하는 초기화 흐름이 필요하다는 기준을 정리했습니다.

또한 퀵슬롯에서 마지막 아이템을 사용했을 때 리스트가 비면서 인덱스 접근 문제가 발생할 수 있었고, 이를 EmptyItem 기본값과 UI 초기화 처리로 보완했습니다.  
이 경험을 통해 UI와 데이터 리스트가 함께 변하는 기능에서는 정상 케이스뿐 아니라, 리스트가 비는 상황과 저장 데이터가 없는 상황까지 고려해야 한다는 점을 배웠습니다.

---

## 8. 이미지 삽입 목록

최종 PDF 또는 PPT 제작 시 다음 이미지를 추가하면 좋습니다.

1. [이미지요청: 게임 대표 플레이 화면]
2. [이미지요청: CSV 아이템 데이터 표]
3. [이미지요청: 인벤토리 UI 전체 화면]
4. [이미지요청: EmptyItem 또는 빈 슬롯이 표시된 인벤토리 화면]
5. [이미지요청: Firebase에 저장된 인벤토리 데이터 화면]
6. [이미지요청: 퀵슬롯 아이템 사용 전/후 비교]
7. [이미지요청: 장비 장착 전/후 캐릭터 또는 장비 UI 비교]

---

## 9. 최종 한 줄 요약

**CSV 기반 아이템 데이터, 인벤토리 UI, Firebase 저장/로드를 연결하면서 저장 데이터 부재와 리스트 초기화 문제를 직접 점검하고, 신규 유저와 기존 유저 모두에서 안정적으로 동작하는 인벤토리 구조의 필요성을 정리한 프로젝트입니다.**
